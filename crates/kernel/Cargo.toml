[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
config = { path = "../config" }
infra= { path = "../infra" }
logger = { path = "../logger" }

chrono = { workspace = true }