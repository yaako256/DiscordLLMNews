[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
config = { path = "../config" }
infra= { path = "../infra" }
logger = { path = "../logger" }
news_fetch = { path = "../news_fetch" }
llm = { path = "../llm" }

tracing = { workspace = true }
chrono = { workspace = true }