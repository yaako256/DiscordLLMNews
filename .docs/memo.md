# .docs/memo.md
# メモ
- Arcで全体共有できる。HTTPクライアントはこれをしたい。

- どうしても無理だからAPIキーもベクトルで持つことにした。  
  今後、`.env`で定義するものは配列になるかもしれない。  
  せっかくだしapiキーもフォールバックにする？

- プログラムが開始した段階で、dataフォルダがあるかを確認。dataフォルダがなかったら作成する処理を追加

- apiキーはモデルと異なり、ミスらなくてもローテーションする仕組みにする。(実際は1つしか設定してないのでローテーションされない)

- AppErrorと同じところでlogger::errorしても意味ない説(帰ってきたところで設定してるから)

---
# 開発日記
## 2026年06月02日(朝)
プロジェクト始動！
- Docker関連の整備
- workspaceの整備
- 設計書の作成
- READMEの作成

準備段階が終わり、いつでも開発を進められる状況になった。

## 2026年06月02日終わり
- エラー型の作成
- 共通型の作成
- configクレートと、config設定の作成

## 2026年06年03日
- ProcessHistoryの型を作成
- infraのI/Oを作成
- loggerを作成(グローバルにした)
- triviaのinfraを作り忘れてたのでそれを作成
- ロガーデバッグ用に仮kernelを作成
- tracingの初期化処理
- NewsFetcherのトレイトの作成
- グローバルなHTTP Clientの作成
- rssの取得部分作成
- 本文の取得部分作成
- idフィルタ部分作成(NewsFetcher構造体の完成)
- llmリクエスト部分完成
- llmリクエスト部分のファイル整理など
- feedのkernelフローは完成(各内部関数はまだ詰めが甘い)
- kernelフローを整理。ログもいい感じにした。
- LivedoorNewsFetcherを整理。ログもいい感じにした。

feed処理は、トリビア履歴関連以外は実行できるようになった。
ver2に比べてコードもちゃんとしてるし、本文もよりきれいに取得(ADや全角スペース等)できてるしいい感じだと思う。





# 次回やること
1. LLMClientの整理。ログもいい感じに
2. infra/storageの整理。ログもいい感じに
3. kernel処理が完全にいい感じになったらトリビア関連を頑張る
4. ついでにプロンプトを軽く考え直す

# 改善点・やること
- triviaの取得と、プロンプトへのreplaceができるようにする。
- 軽くプロンプトを考える。(深く詰めるのは後)
- ファイルのI/O処理などをちゃんとして、feedのフローを完遂できるようにする。
- 通常ログ(tracing)と通知用ログ(logger)のちゃんとした記入をしていきつつ、コメントをちゃんとさせる。

ここで、完全に feedフロー完了

- sendフローの作成を進めていく。
- 本文の取得のIDをぐるぐるにする