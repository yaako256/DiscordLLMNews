# .docs/memo.md
# メモ
- Arcで全体共有できる。HTTPクライアントはこれをしたい。

- どうしても無理だからAPIキーもベクトルで持つことにした。  
  今後、`.env`で定義するものは配列になるかもしれない。  
  せっかくだしapiキーもフォールバックにする？

- プログラムが開始した段階で、dataフォルダがあるかを確認。dataフォルダがなかったら作成する処理を追加

- apiキーはモデルと異なり、ミスらなくてもローテーションする仕組みにする。(実際は1つしか設定してないのでローテーションされない)

- AppErrorと同じところでlogger::errorしても意味ない説(帰ってきたところで設定してるから)

- jsonのパースエラーは改行のエスケープで大体起きているらしい。「文字列中の改行は \n にエスケープしてください」をプロンプトに追加する
---
# 開発日記
## 2026年06月02日(朝)
プロジェクト始動！
- Docker関連の整備
- workspaceの整備
- 設計書の作成
- READMEの作成

準備段階が終わり、いつでも開発を進められる状況になった。

## 2026年06月02日終わり
- エラー型の作成
- 共通型の作成
- configクレートと、config設定の作成

## 2026年06年03日
- ProcessHistoryの型を作成
- infraのI/Oを作成
- loggerを作成(グローバルにした)
- triviaのinfraを作り忘れてたのでそれを作成
- ロガーデバッグ用に仮kernelを作成
- tracingの初期化処理
- NewsFetcherのトレイトの作成
- グローバルなHTTP Clientの作成
- rssの取得部分作成
- 本文の取得部分作成
- idフィルタ部分作成(NewsFetcher構造体の完成)
- llmリクエスト部分完成
- llmリクエスト部分のファイル整理など
- feedのkernelフローは完成(各内部関数はまだ詰めが甘い)
- kernelフローを整理。ログもいい感じにした。
- LivedoorNewsFetcherを整理。ログもいい感じにした。

feed処理は、トリビア履歴関連以外は実行できるようになった。
ver2に比べてコードもちゃんとしてるし、本文もよりきれいに取得(ADや全角スペース等)できてるしいい感じだと思う。

# 2026年06月06日
- LLMClientやinfra/storageの整理をして、ログもいい感じにしたつもり
- feedフローの完成。送信用ログもいい感じにした。
- sendフローの完成。ちゃんとstatusをsentにしたりした。
- アプリ内時間を日本時間にした。chronoは基本UTCしかできない。

# 2026年06月07日
- CLI部分を完成させた。feed実行→send実行で1連の流れを実行可能に。
- dataをvolume化した。
- dataのjsonがなかったらErr終了してたのを、ログ通知&OK終了に修正した。
- 仮crontabを作成。`make deploy`をしてみたため、トリビアなし仮運用をしてみる

**トリビアなし仮運用チェック開始。自鯖に21時にsendされる**
(ほんとはこのまま数日放置するつもりだったが、暇ができたので作成を進めた)

- 時刻をreplaceでちゃんと指定した。(固定値になってた)
- Trivia関連を頑張った。(ロード、記録、プロンプトへのreplace)(結局Summaryをjsonで出力してもらうようにした)
- Triviaを含めたプロンプトを考えた。(これだけまだ再考の余地あり。ただ、すでにだいぶいいので後回し)(絵文字などがなくなってしまった問題もあったが、それらも修正し、いい感じになっている)
- trivia_historyのjsonlの読み込みの仕組みが間違っていた問題の修正
- devコンテナにもlogの最大容量を設定した
- プロンプトに渡す日付を日本曜日にするための関数を作成した
- ニュース選定フェーズのプロンプトを再考した。これのせいでニュースに抜けができ、少ないニュースだけとなっていた。

# 2026年06月10日
- 本文の取得のIDを、よりサイト側に負荷がかからないよう最適化した。
start_idを自分で指定するのをやめて、CATEGORY_UNITでid設定をするように変更した。
カテゴリ順とその中身もランダムにして、取得順をバラ消させた。
- プレースホルダーが直地になっていたので、定数にした。
- パッチ通知関連とそのCLIコマンドが完成した。
二重送信を抑える仕組みなどは甘いが十分活用可能な段階である。
- ファイルが存在しない時のsendエラーでprocess_historyが記入されない問題の修正
→でも多分dataフォルダがないときはエラーになるまま？詳しくはわからない

明日は以下のことをする
- 全体チェック
- prodのTrivia_historyが良くないので、devのを移し替える
- configなども移し替える
- 本運用開始
- パッチ通知をしてみる
- Githubのリリースをする

# 2026年06月11日
- 全体チェックを行った。仮定数のconfig化や不要ログの削除など、軽微な修正を色々行った。
- prodのTrivia_historyが良くないので、devのを差し替えた。日付も変更し、1日おきに実行している風にした。
- configを本番用に設定した。
- crontabも本番用に設定した。
- envも本番用に設定した。

**本運用開始**

- パッチ通知をした
- Githubのリリースをする

# 2026年06月12日
- 正常終了しているのに定刻送信がされなかった問題の修正
- ついでにパッチ通知をしたログがログwebhookに送信されていなかった問題の修正
- その内容をv3.0.1としてパッチ通知しつつ、再deployした。



# 改善点・やること
- ☑本文の取得のIDをぐるぐるにする
- ☑パッチ通知のCLIコマンドの追加
- ☑プレースホルダーが直値。修正する。
- ☑ファイルが存在しない時のsendエラーでprocess_historyが記入されない問題の修正
- ☑パッチ通知したことをlogに示す機能がkernelに反映されてなく、使われてない問題の修正

- ニュースURLも張り、ジャンプできるようにする
- トリビア履歴などを初期化(prune)するCLIコマンドの追加 → 正味1日1回だしいらない
- 1,2回目のLLMリクエストの出力要素をidだけでなく、カテゴリも含ませることで、LLMがカテゴリを考えてくれる説
- rust側でrandして、豆知識を新たな奴か深堀か、曜日ありかとかやってもいいかもね。
- パッチ通知システムの二重送信防止などの細かな処理(現在はsendを2回連続で実行できないだけ)
- ~~_history.jsonl関連をSQL DBにしたっていい
- 最終版の設計書作成
- .gitattributesを使った、gitのダウンロード時に作られるzipへの制限(日記は含めないなど)を行う
- crontabもvolume化する？(現在はcopyをしているため、再deployしないと反映されない)
- パッチノートは`.config/prod/patch_note/v3.0.1.md`みたいに階層を下げてもいいかもしれない。てかそれがいいかも